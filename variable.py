"""
VAriables = it is a container for storing the data

Rules while defining variables :

1. We cannot start a variable name with any numbers
2. We can use alphabet,numbers,underscore in variable names.
3. We cannot use any type of special symbols. (@,#,$,%,^,&,*,(,),-,+,=,/,|,\,<,>,?,~)
4. we cannot use whitespace in variable names. We can use underscore instead of whitespace.
5. We cannot use predefined keywords as variable names. 

"""
a_1 = 1
name = "Ram"
first_name = "Shyam"

print(a_1)
print(name)
print(first_name)   

